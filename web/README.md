# /web — Vue 3 web app

Placeholder. It gets scaffolded in **Phase 3 (Web core)**, see `PRD.md` § 7.

Planned stack: Vue 3 (Composition API) + Vite + TypeScript, Vue Router, PrimeVue + Tailwind, `supabase-js`, deployed to Vercel as a static SPA.
The app holds only the Supabase URL and the anon/publishable key, never the service_role key.
Local dev runs on `http://localhost:5173`, which is the URL already allow-listed for auth redirects.
